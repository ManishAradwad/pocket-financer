// Mock for the @env module created by react-native-dotenv.
// Pocket-Financer does not rely on any env vars at runtime; this file exists
// so that jest can still resolve `@env` for any transitive imports.
export {};
